{{#each ideas}}
  <div class='card card-body mb-3'>
    <h4>{{title}}</h4>
    <p>{{details}}</p>
    <a href='/ideas/edit/{{_id}}' class='btn btn-dark btn-block'>Edit</a>
  </div>
{{else}}
  <p class='alert alert-warning'>No video ideas listed</p>
{{/each}}